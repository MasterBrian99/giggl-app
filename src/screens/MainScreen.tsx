import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const MainScreen = () => {
  return (
    <View>
      <Text>Home Screen Main</Text>
    </View>
  );
};

export default MainScreen;

const styles = StyleSheet.create({});
